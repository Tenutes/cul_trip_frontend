export const recommendations = ({ recommendations }) => recommendations;
export const newEvents = ({ newEvents }) => newEvents;
export const compilations = ({ compilations }) => compilations;
export const fetching = ({ fetching }) => fetching;
